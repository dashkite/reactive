# Testing

## Overview

This project uses standard testing frameworks to verify the reactive primitives. Amen's native support for iterators allows us to construct a testing strategy as implementation. Since the reactive primitives are built on iterators, developers test them by interacting with them just as they operate in production.

## Running Tests

To execute the test suite, run the following command:

```bash
npx genie test
```
